$if(has-frontmatter)$
\frontmatter
$endif$
$if(title)$
$if(beamer)$
\frame{\titlepage}
$else$
\begin{singlespace}
    \maketitle
\end{singlespace}
$endif$
$if(abstract)$
\section*{Abstract}
$abstract$
$endif$
$endif$
\thispagestyle{plain}

\fancyhead[LO]{\MakeUppercase{\textbf{$shorttitle$}}}
\fancyhead[RE]{\MakeUppercase{\textbf{$for(authors/first)$$it.name.family$$endfor$ et al.}}}