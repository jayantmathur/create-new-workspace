{
  "name": "doc",
  "version": "0.0.1",
  "description": "A quarto document",
  "scripts": {
    "dev": "quarto preview pages/index.qmd --port 4000",
    "build": "quarto render",
    "push": "pnpm version --force"
  }
}
